file(GLOB SYMLINKS ${CMAKE_CURRENT_SOURCE_DIR}/*ns*)
foreach(SYMLINK_PATH IN LISTS SYMLINKS)
	get_filename_component(SYMLINK ${SYMLINK_PATH} NAME)
	install(FILES ${SYMLINK} DESTINATION ${CMAKE_INSTALL_BINDIR})
endforeach(SYMLINK_PATH)
